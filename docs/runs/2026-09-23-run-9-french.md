# 2026-09-23, run 9: French, the same suite as run 8, 19 of 19 green

`sans-facultatifs`, `-Language French`, through `Run-PickleWsl.ps1`, staged from `9df3305`, the revision run 8 was
staged from. Same mod list (Core, the five DLC, Harmony, RimLogging, Pickle, Dubs Bad Hygiene 3.1.2800, MMDrumcanMOD
(Continued), the companion `Drum Bath Hygiene - Pickle tests`), the game in French.

| `exitReason` | Played of written | Passed | Failed | Skipped |
| --- | --- | --- | --- | --- |
| **passed** | 19 of 19 | 19 | 0 | 0 |

No `@wip`, no `@requires:`, every scenario once, no retry. Wrapper exit code 0, no `No data available`.

## Scenarios

All nineteen passed, from 10 s to 30 s each (the slowest again the two-baths scenario, 30.2 s, and the save-and-reload,
28.1 s). Same outcome per scenario as run 8; the per-scenario lines are in `summary.md` beside the Evidence. No scenario
spells an English label, so the features are the ones run 8 played.

## The log

`Player.log` holds one `[ERROR]`, at start-up, from the game and not from a scenario: "Mod Drum Bath Hygiene - Pickle
tests did not load any content". The companion mod carries a step assembly and features and no def, and RimWorld says
so; the English log of run 8 holds the same single line. Every scenario ends on `no errors were logged` and on `no warnings from
mod "Drum Bath Hygiene"`, and both held.

## The capture and the film, opened

- **Still** (`manual--bather-in-the-drum--step0.png`, 1920x1080, whole frame): the colonist sits in the red drum, the fire
  lit under it, tooltip "Bather, Concierge (femme colon), Sain", inspect panel in French reading "Se détendre dans le
  bain en regardant passer les nuages." (the same line as run 8's English still, translated). This is the image run 7's
  French capture was not: the bath is what the panel says and what the picture shows.
- **Film** (17.1 s, sampled every 3 s): loading screens in French, then the colonist crossing the map with that same
  panel line, then the drum, as in run 8.

## Scope

The text on screen during the bath is Dubs Bad Hygiene's and the drum mod's, not this mod's (`STATUS.md`, translation
fields): what the two stills show is that both languages render it, not that this repository translates anything.

## Evidence

`.build/evidence/tested-french/`: `summary.json`, `summary.md`, `report.html.gz`, `messages.ndjson.gz`,
`Player.log.gz`, `junit.xml.gz`, the still (3.2 MB) and the film (0.3 MB), 7.4 MB in all. The small copies of runs 6 and 7
were deleted the same day: this run and run 8 replace them for the revision now in the repository.
